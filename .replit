deploy.run = ["npm", "start"]

run = ["npm", "run", "dev"]

language = "nodejs"
entrypoint = "package.json"
modules = ["nodejs-20"]

onBoot = "npm install"

[packager]
language = "nodejs"
ignoredPackages = ["**/node_modules"]
packageSearch = true
packageIgnore = ["**/node_modules"]


run = "npm start"

[deployment]
run = ["npm", "start"]
build = ["sh", "-c", "npm install && npm run build"]

language = "nodejs"
entrypoint = "package.json"

[packager]
language = "nodejs"

[env]
PORT = "5000"
